version: 2
jobs:
  build:
    docker:
      - image: node:7
    working_directory: ~/qliktive-custom-analytics
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install --quiet
          working_directory: ~/qliktive-custom-analytics/test
      - run:
          name: Run ESLint
          command: npm run lint
          working_directory: ~/qliktive-custom-analytics/test
      - run:
          name: Install Docker Compose
          command: |
            set -x
            curl -L https://github.com/docker/compose/releases/download/1.11.2/docker-compose-`uname -s`-`uname -m` > /usr/local/bin/docker-compose
            chmod +x /usr/local/bin/docker-compose
      - run:
          name: Validate docker-compose files
          command: npm run validate:compose
          working_directory: ~/qliktive-custom-analytics/test
      - run:
          name: Install docker client
          command: |
            curl -L -o /tmp/docker-17.04.0-ce.tgz https://get.docker.com/builds/Linux/x86_64/docker-17.04.0-ce.tgz
            tar -xz -C /tmp -f /tmp/docker-17.04.0-ce.tgz
            mv /tmp/docker/* /usr/bin
      - setup_remote_docker
      - run:
          name: Run E2E tests on a local setup
          command: |
            # Create certificates
            ../scripts/create-certs.sh -a localhost

            # Spin up a local setup
            docker login -u "$DOCKER_USER" -p "$DOCKER_PASSWORD"
            docker-compose -f ../docker-compose.yml -f ../docker-compose.cci.yml up -d

            # Copy data and doc files into engine container
            docker cp ../data/csv/. qix-engine:/data
            docker cp ../data/doc/. qix-engine:/doc

            # Copy certs into openresty container for TLS
            docker cp ../secrets/cert-gateway.crt openresty:/secrets
            docker cp ../secrets/cert-gateway.key openresty:/secrets

            # Properly start openresty. Cannot be done earlier as it needs the certs
            docker exec -d openresty /usr/local/openresty/nginx/entrypoint.sh

            # Find out ip address of gateway
            CONTAINER_ID=$(docker ps -aqf "name=qliktivecustomanalytics_openresty_1")
            TEST_HOST=$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' "$CONTAINER_ID")

            # Build docker image for test
            docker build --build-arg TEST_HOST="$TEST_HOST" -t test/test-e2e .

            # Execute tests
            docker run --rm test/test-e2e
          working_directory: ~/qliktive-custom-analytics/test
